name: expense_tracker_server
description: Backend API server for Expense Tracker app
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  mongo_dart: ^0.10.3
  crypto: ^3.0.3
  uuid: ^4.2.1
